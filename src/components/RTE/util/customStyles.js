import { Inline } from './constants'

/* Custom styles for custom entities like HIGHLIGHT. */
const customStyles = {
  [Inline.HIGHLIGHT]: {
    backgroundColor: 'yellow',
  }
}

export default customStyles
